---
type Props = {
	icon: string,
	text: string
}

const {icon, text} = Astro.props

---
<div class="rounded-2xl bg-white p-4 shadow-sm"><span class="text-3xl">{icon}</span>
	<p class="mt-2 text-xs font-bold text-navy">{text}</p>
</div>